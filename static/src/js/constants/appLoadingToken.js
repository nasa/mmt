/**
 * This token represents the app being in a 'loading' state, when we need to wait for a real token
 * to be read from local storage.
 */
const APP_LOADING_TOKEN = 'LOADING'

export default APP_LOADING_TOKEN
