<!DOCTYPE html>
<!--[if lt IE 7]><html class="no-js lt-ie9 lt-ie8 lt-ie7"  lang="en"> <![endif]-->
<!--[if IE 7]><html class="no-js lt-ie9 lt-ie8"  lang="en"> <![endif]-->
<!--[if IE 8]><html class="no-js lt-ie9"  lang="en"> <![endif]-->
<!--[if gt IE 8]><html class="no-js" lang="en"> <!--<![endif]-->
<head>
  <title>Metadata Management Tool</title>

  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="description" content="">

  <%= csrf_meta_tag %>

  <!-- Stylesheets -->
  <%= stylesheet_link_tag 'https://fonts.googleapis.com/css?family=Open+Sans:400,700', media: 'all' unless Rails.env.test? %>
  <%= stylesheet_link_tag 'application', media: 'all' %>

  <!-- Javascripts -->
  <%= javascript_include_tag 'application' -%>

  <% if Rails.configuration.respond_to?(:analytics_id) -%>
    <!-- Google Analytics -->
    <%= render 'shared/google_analytics' %>
  <% end -%>

  <% if Rails.env.test? %>
  <!-- Prevent CSS animations in test -->
  <style type="text/css">
    * {
     transition-property: none !important;
     -o-transition-property: none !important;
     -moz-transition-property: none !important;
     -ms-transition-property: none !important;
     -webkit-transition-property: none !important;

     transform: none !important;
     -o-transform: none !important;
     -moz-transform: none !important;
     -ms-transform: none !important;
     -webkit-transform: none !important;

     animation: none !important;
     -o-animation: none !important;
     -moz-animation: none !important;
     -ms-animation: none !important;
     -webkit-animation: none !important;
    }
  </style>

  <!-- Prevent jQuery animations in tests -->
  <%= javascript_tag '$.fx.off = true;' if Rails.env.test? -%>
  <% end -%>
</head>

<body class="<%= (controller.action_name == 'manage_metadata' || controller.action_name == 'manage_cmr' ? 'dashboard' : '') %>">
  <a href="#main-content" class="skip-nav">Skip to main content</a>
  
  <div class="wrapper">
    <%= render 'shared/header' %>

    <div id="main-content">
      <main class="internal" role="main">
        <%= yield %>
      </main>
    </div>

    <div class="push">
      <!-- For pushing the footer to the bottom of a page with minimal content -->
    </div>
  </div>

  <%= render 'shared/footer' %>

  <%= javascript_include_tag "netinsight_2.3/ntpagetag" unless Rails.env.test? %>
</body>
</html>
