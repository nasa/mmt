<%= @user[:name] %>,

<%= "The collection draft proposal #{@short_name}_#{@version} has been reviewed and rejected#{@reasons_text}." %>

<% if @rejected_note %>
  <%= simple_format("Additional feedback was provided: '#{@rejected_note}'") %>
<% else %>
  <%= "Please consult #{@dmmt_user_guide_url} for information on CMR metadata best practices." %>
<% end %>

View Collection: <%= collection_draft_proposal_url(@id) %>
