<script>
  // Load json-schema for field validation
  var globalJsonSchema = <%= raw @json_schema.to_json %>;

  // var urlContentTypeMap = <%#= raw @umm_c_related_url_mapping.to_json %>;
  <% if form == 'related_urls' %>
    var urlContentTypeMap = <%= raw @umm_c_related_url_mapping.to_json %>;
  <% elsif form == 'data_centers' %>
    var urlContentTypeMap = <%= raw @data_center_related_url_mapping.to_json %>;
  <% elsif form == 'data_contacts' %>
    var urlContentTypeMap = <%= raw @data_contact_related_url_mapping.to_json %>;
  <% end %>

  <% if controller_name == 'collection_templates' %>
    var globalTemplateNames = <%= sanitize @template_names.to_json %>;
  <% end %>
</script>
