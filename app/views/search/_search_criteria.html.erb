<p class="search-meta">
  <strong>
    <%= (@records.total_count == 0) ? 'No matches found' : pluralize(@records.total_count, "#{@record_type.titleize.singularize} Result") %>
  </strong>
  <%= results_query_label(@query) %>
</p>
