
<p class="search-meta">
  <% if !@entry_id.blank? %>
    <strong><%= (@total_hit_count==0) ? 'No matches found' : pluralize(@total_hit_count, 'Result') %></strong>
    for: Entry Id: "<%= @entry_id %>"
  <% end %>
</p>
