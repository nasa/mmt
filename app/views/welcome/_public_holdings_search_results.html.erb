<table id='public-holdings'>
  <thead>
  <tr>
    <th>Provider Name</th>
    <th>Collections</th>
    <th>Granules</th>
  </tr>
  </thead>
  <tbody>
  <% if @table_rows.blank? %>
    <tr>
      <td colspan="4">
        No public holdings found
      </td>
    </tr>
  <% else %>
    <% @table_rows.each do |row| %>
      <tr class='<%= cycle("alt", "") %>'>
        <td>
          <%= link_to row['name'], collections_path(id: row['provider-id']) %>
        </td>
        <td>
          <%= number_with_delimiter(row['collection-count']) %>
        </td>
        <td>
          <%= number_with_delimiter(row['granule-count']) %>
        </td>
      </tr>
    <% end %>
  <% end %>
  </tbody>
</table>
