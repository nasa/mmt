<% flash.each do |type, message| %>
  <div class="banner banner-dismissible <%= flash_class(type) %>">
    <%= message %>
    <a href="javascript:void(0)" class="close" title="Dismiss message"><i class="fa fa-close"></i></a>
  </div>
<% end %>
