<% flash.each do |type, message| %>
  <div class="banner banner-dismissible <%= flash_class(type) %>">
    <%= message.html_safe %>
    <a href="javascript:void(0)" class="dismiss" title="Dismiss message"><i class="eui-icon eui-fa-times-circle"></i></a>
  </div>
<% end %>
