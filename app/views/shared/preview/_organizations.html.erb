<div class="preview-collection accordion is-closed">
  <div class="accordion-header">
    <h4>Organizations Fields:</h4>
    <a href="#" class="fl-right">
      <i class="fa fa-angle-down toggle"></i>
      <span class="hidden">Toggle Organizations Fields</span>
    </a>
  </div>
  <div class="accordion-body">
    <ul class="organizations-preview">
      <h4>Organizations <% if @draft.present? %><%= link_to '<i class="fa fa-edit"></i> <span class="hidden">Edit Organizations</span>'.html_safe, draft_edit_form_path(@draft, 'organizations', {anchor: 'organizations'}), {class: 'hash-link'} %><% end %></h4>
      <%= render partial: 'shared/preview/preview_type', locals: {
          data: metadata['Organizations'],
          name: 'Organizations',
          type: TypesHelper::ResponsibilityType
      } %>

    </ul>
  </div>
</div>
