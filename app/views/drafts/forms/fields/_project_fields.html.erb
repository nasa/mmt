<div class="row sub-fields">
  <!-- Short Name [r] -->
  <label for="<%= remove_pipes(name_prefix) %>short_name" class="icon-required">Short Name</label><i class="fa fa-info-circle"></i>
  <%= text_field_tag(name_to_param("#{name_prefix}|short_name|"), project['ShortName'], class: 'half-width') %>


  <!-- Long Name -->
  <label for="<%= remove_pipes(name_prefix) %>long_name">Long Name</label><i class="fa fa-info-circle"></i>
  <%= text_field_tag(name_to_param("#{name_prefix}|long_name|"), project['LongName'], class: 'half-width') %>


  <!-- Campaign -->
  <% campaigns = project['Campaign'] || [nil] # [nil] creates at least one field %>
  <div class="multiple campaign">
  <% campaigns.each do |campaign| %>
    <div class="multiple-item">
      <label for="<%= remove_pipes(name_prefix) %>campaign_">Campaign</label><i class="fa fa-info-circle"></i>
      <%= text_field_tag(name_to_param("#{name_prefix}campaign_index"), campaign, class: 'half-width') %>
      <div class="actions">
        <button type="button" class="btn btn-green btn-sm btn-round add-new new-simple">Add another</button>
        <button type="button" class="btn btn-red btn-sm btn-round remove">Remove</button>
      </div>
    </div>
  <% end %>
  </div>


  <!-- StartDate -->
  <label for="<%= remove_pipes(name_prefix) %>start_date">Start Date</label><i class="fa fa-info-circle"></i>
  <%= datetime_field_tag(name_to_param("#{name_prefix}|start_date|"), project['StartDate'], class: 'half-width') %>


  <!-- EndDate -->
  <label for="<%= remove_pipes(name_prefix) %>end_date">End Date</label><i class="fa fa-info-circle"></i>
  <%= datetime_field_tag(name_to_param("#{name_prefix}|end_date|"), project['EndDate'], class: 'half-width') %>
</div>
