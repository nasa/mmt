<div class="row sub-fields">
  <!-- Longitude -->
  <label for="<%= remove_pipes(name_prefix) %>longitude" class="icon-required">Longitude</label><i class="fa fa-info-circle"></i>
  <%= number_field_tag(name_to_param("#{name_prefix}longitude"), point['Longitude'], class: 'half-width longitude', step: 'any') %>


  <!-- Latitude -->
  <label for="<%= remove_pipes(name_prefix) %>latitude" class="icon-required">Latitude</label><i class="fa fa-info-circle"></i>
  <%= number_field_tag(name_to_param("#{name_prefix}latitude"), point['Latitude'], class: 'half-width latitude', step: 'any') %>

</div>
