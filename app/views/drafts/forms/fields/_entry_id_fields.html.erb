<div class="row sub-fields">
  <label for="<%= remove_pipes(name_prefix) %>id" class="icon-required">ID</label><i class="fa fa-info-circle"></i>
  <%= text_field_tag(name_to_param("#{name_prefix}id"), entry_id['Id'], class: 'half-width') %>

  <label for="<%= remove_pipes(name_prefix) %>version">Version</label><i class="fa fa-info-circle"></i>
  <%= text_field_tag(name_to_param("#{name_prefix}version"), entry_id['Version'], class: 'half-width') %>

  <label for="<%= remove_pipes(name_prefix) %>authority">Authority</label><i class="fa fa-info-circle"></i>
  <%= text_field_tag(name_to_param("#{name_prefix}authority"), entry_id['Authority'], class: 'half-width') %>
</div>
