<main class="dashboard" role="main">
  <article id="metadata" class="row-content flex-row--start" role="article">
    <% if @non_nasa_approver %>
      <%= render partial: 'proposal/proposal_partials/manage_proposals_approver' %>
    <% else %>
      <%= render partial: 'proposal/proposal_partials/manage_proposals_user' , locals: {
        create_type: 'Collection Proposal',
        search_type: 'collections'
      } %>
    <% end %>
  </article>
</main>
