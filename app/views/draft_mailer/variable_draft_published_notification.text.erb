<%= @user[:name] %>,
<%= "Your variable metadata record #{@short_name} has been successfully #{ @is_update ? 'updated' : "published to the CMR#{@email_env_note}"}. Your variable's concept ID is #{@concept_id}." %>
View Variable: <%= variable_url(@concept_id, revision_id: @revision_id) %>
