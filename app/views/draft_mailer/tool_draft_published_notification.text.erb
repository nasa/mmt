<%= @user[:name] %>,
<%= "Your tool metadata record #{@short_name} has been successfully #{ @is_update ? 'updated in' : 'published to'} the CMR#{@email_env_note}. Your tool's concept ID is #{@concept_id}." %>
View Service: <%= tool_url(@concept_id, revision_id: @revision_id) %>
