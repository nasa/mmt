<%= @user[:name] %>,
<% env = Rails.configuration.cmr_env.upcase %>
<% if env != 'OPS'%>
<%= "Your service metadata record #{@short_name} has been successfully #{ @is_update ? 'updated' : "published to the CMR (#{env})"}. Your service's concept ID is #{@concept_id}." %>
<% else %>
<%= "Your service metadata record #{@short_name} has been successfully #{ @is_update ? 'updated' : 'published to the CMR'}. Your service's concept ID is #{@concept_id}." %>
<% end %>
View Service: <%= service_url(@concept_id, revision_id: @revision_id) %>
