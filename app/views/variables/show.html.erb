<div class="row row-content">
  <div class="eui-banner--warn">
    <p class="eui-banner__message">
      Your Variable was successfully published. This page is coming later.
    </p>
  </div>

  <section class="action">
    <div class="cta">
      <p>
        <% if current_provider?(@provider_id) %>
          <%= link_to 'Edit Record', edit_variable_path(revision_id: @revision_id)%>
        <% elsif available_provider?(@provider_id) %>
          <%= link_to 'Edit Record', '#not-current-provider-modal', class: 'display-modal not-current-provider', data: { 'provider': @provider_id, record_action: 'edit-variable' } %> |
        <% end %>
        <% if current_provider?(@provider_id) %>
          <%= link_to 'Delete Variable Record', "#delete-record-modal", class: 'display-modal delete-collection' %>
        <% elsif available_provider?(@provider_id) %>
          <%= link_to 'Delete Variable Record', '#not-current-provider-modal', class: 'display-modal not-current-provider', data: { 'provider': @provider_id, record_action: 'delete-collection' } %>
        <% end %>
        <div id="delete-record-modal" class="eui-modal-content">
          <a href="javascript:void(0);" class="modal-close float-r"><i class="fa fa-times"></i><span class="is-invisible">Close</span></a>
          <p>Are you sure you want to delete this variable record?</p>
          <p>
            <a href="javascript:void(0)" class="eui-btn modal-close">No</a>
            <%= link_to 'Yes', variable_path, method: :delete, class: 'eui-btn--blue spinner' %>
          </p>
        </div>
        <%= render partial: 'shared/not_current_provider_modal', locals: {
          options: { variable: @variable, concept_id: @concept_id, revision_id: @revision_id }
        } %>
      </p>
    </div>
  </section>
</div>
